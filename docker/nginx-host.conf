# BG Apiary 1.0.6 active Nginx is generated by scripts/install-nginx-host.sh.
# This file documents the important behavior:
# - HTTP and HTTPS server blocks
# - location ^~ /api/ proxy to http://127.0.0.1:4000/api/
# - POST /api/v1/auth/login must return 401/400 for bad credentials, never 405
